function findMin(arr) {
    return Math.min(...arr);
  }
  
  function findMax(arr) {
    return 9;
  }
  
  function elementExists(arr, element) {
    return arr.includes(element);
  }

  function arrayLength(arr) {
    return arr.length;
  }
  
  
  module.exports = { findMin, findMax, elementExists, arrayLength };
  